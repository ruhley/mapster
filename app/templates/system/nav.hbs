<nav class='navbar navbar-inverse navbar-fixed-top'>
      <div class='container'>
        <div class='navbar-header'>
            {{#link-to 'index' class='navbar-brand'}}Mapster <span class='label label-primary'>Beta</span>{{/link-to}}
            <button data-target='#navbar-main' data-toggle='collapse' type='button' class='navbar-toggle'>
                <span class='icon-bar'></span>
                <span class='icon-bar'></span>
                <span class='icon-bar'></span>
          </button>
        </div>
        <div id='navbar-main' class='navbar-collapse collapse'>
            <ul class='nav navbar-nav'>
                {{#if model.entities}}
                    <li class='dropdown'>
                      <a id='entities' href='#' data-toggle='dropdown' class='dropdown-toggle'>Entities <span class='caret'></span></a>
                        <ul aria-labelledby='entities' class='dropdown-menu'>
                            {{#each entity in model.entities}}
                                <li>{{#link-to 'entity' entity.url}}{{entity.plural}}{{/link-to}}</li>
                            {{/each}}
                        </ul>
                    </li>
                {{/if}}

                {{#if model.items}}
                    <li class='dropdown'>
                      <a id='items' href='#' data-toggle='dropdown' class='dropdown-toggle'>{{model.entity.plural}} <span class='caret'></span></a>
                        <ul aria-labelledby='items' class='dropdown-menu'>
                            <li>{{#link-to 'entity.info' model.entity.url 0}}Create{{/link-to}}</li>
                            <li class='divider'></li>
                            {{#each item in model.items}}
                                <li>{{#link-to 'entity.info' model.entity.url item.url}}{{item.name}}{{/link-to}}</li>
                            {{/each}}
                        </ul>
                    </li>
                {{/if}}
            </ul>

            <ul class='nav navbar-nav navbar-right col-sm-3'>
                <li style='margin:0.5em;'>
                    <div class='input-group'>
                        <input type='search' class='form-control' id='search'>
                        <span class='input-group-btn'>
                            <button class='btn btn-default'>
                                <i class='fa fa-search'></i>
                            </button>
                        </span>
                    </div>
                </li>
            </ul>

        </div>
    </div>
</nav>
