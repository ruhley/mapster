{{#if circle}}
  <circle cx="{{cx}}" cy="{{cy}}" r="{{r}}" class="{{class}}" id="{{id}}" data-id="{{data_id}}"></circle>
{{else}}
  <path d="{{d}}" class="{{class}}" id="{{id}}" data-id="{{data_id}}"></path>
{{/if}}
