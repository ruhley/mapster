{{#if items}}
    {{#each item in items}}
        {{#compare _view.contentIndex '<' limit}}
            {{item-list-item item=item link=link}}
        {{/compare}}
    {{/each}}
{{else}}
    None found
{{/if}}
